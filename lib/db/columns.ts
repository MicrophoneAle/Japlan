// Column lists shared by every trips query, so a new column is added once.
export const TRIP_COLS =
  "id, linq_chat_id, name, destination, start_date, end_date, state, difficulty, stake_text, timezone, destination_profile_json, is_solo, daily_points_cap, organizer_participant_id, setup_state, completed_at, board_time";
